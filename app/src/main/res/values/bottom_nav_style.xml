<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 🧭 Bottom Navigation Style -->
    <style name="Widget.MyApp.BottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView.Colored">
        <item name="backgroundTint">?attr/colorSurface</item>
        <item name="itemIconTint">?attr/colorOnPrimary</item>
        <item name="itemTextColor">?attr/colorOnPrimary</item>
        <item name="elevation">8dp</item>
    </style>

    <!-- 🧱 Top App Bar (Toolbar) Style -->
    <style name="Widget.MyApp.TopAppBar" parent="Widget.MaterialComponents.Toolbar.PrimarySurface">
        <item name="background">@color/black</item>
        <item name="titleTextColor">@color/light_primary</item>
        <item name="subtitleTextColor">@color/white</item>
        <item name="navigationIconTint">@color/design_default_color_primary</item>
        <item name="elevation">4dp</item>
    </style>

    <!-- 🗂️ TabLayout Style -->
    <style name="Widget.MyApp.TabLayout" parent="Widget.MaterialComponents.TabLayout.Colored">
        <item name="tabBackground">?attr/colorSurface</item>
        <item name="tabTextColor">?attr/colorOnPrimary</item>
        <item name="tabSelectedTextColor">?attr/colorOnPrimary</item>
        <item name="tabIndicatorColor">?attr/colorOnPrimary</item>
        <item name="elevation">4dp</item>
    </style>

</resources>